如果你要自己實作這篇論文的方法，可以把整個系統當成「一個模組化的 ML 專案」，一步一步搭起來，而不是一次就寫成一個大檔案。下面我用 **Python 為主（NumPy / SciPy / scikit-learn / MNE）** 幫你規劃整個實作流程和程式架構，也會提醒你怎麼寫才有效率。整體邏輯是完全依照論文中的架構：S/F/T 細粒度切片 → divCSP 特徵 → Wrapper feature selection → 線性 SVM ensemble。

---

## 0. 開發環境與專案結構建議

**語言／套件建議（Python）：**

* `numpy`, `scipy`（線性代數、濾波）
* `scikit-learn`（Linear SVM, cross-validation）
* `mne`（讀 EEG、處理試次）
* 可選：`joblib` 或 `multiprocessing`（平行運算加速）
* 若要嚴格實作 divCSP 的流形最佳化，可用 `pymanopt` / `geomstats`（在 Stiefel / orthogonal manifold 上做 gradient descent）

**專案目錄大致可以長這樣：**

```text
fgsft_bci/
  config.py              # 參數設定
  data_loader.py         # 讀資料、切 trial、標籤、block index
  preprocessing.py       # 重取樣、濾波、標準化
  segmentation.py        # 時間/頻率/空間 分割 & 建 SFTS 定義
  divcsp.py              # 實作 divergence-based CSP
  features.py            # 從單一 SFTS 萃取 divCSP 特徵
  feature_selection.py   # Algorithm 2 (wrapper-based SFTS selection)
  model.py               # ensemble SVM 訓練與推論 (Algorithm 3)
  metrics.py             # accuracy + ITR
  viz.py                 # MI-TFRM & time-frequency topo map (可選)
  main_train.py          # 主訓練腳本
  main_eval.py           # 測試 / demo
```

---

## 1. 資料與前處理模組設計

### 1.1 資料結構設計

假設針對「單一受試者」進行訓練（原文也是 subject-specific）：

* `X`：shape = `(n_trials, n_channels, n_samples)`
* `y`：shape = `(n_trials,)`，值為 0 / 1（二分類 MI）
* `blocks`：shape = `(n_trials,)`，紀錄每個 trial 所屬的 block（用來做 LOBO cross-validation）
* 採樣頻率 `fs = 250 Hz`（論文將兩個資料庫都重採樣到 250 Hz）

### 1.2 前處理步驟（`preprocessing.py`）

1. **重取樣到 250 Hz**
2. **帶通濾波**

   * 原文 BCI IV 2a：0.5–100 Hz，並加 50 Hz notch。
   * 若你不用原始 raw，而是已 preprocessed 的版本，可略過。
3. **不刪 trial，不刪 channel**

   * 論文刻意不刪除，模擬真實情境。
4. **試次切片**

   * 以 BCI IV 2a 為例：他們取 **刺激後 2–5 秒** 的資料（3 秒），並在時間實驗裡比較用 1s、1.5s… 等長度。
   * 你可以先固定用 0–1.5 s（相對於你切出的 1.5 秒片段的起點）做主實驗。

---

## 2. S/F/T 細粒度分割規劃（`segmentation.py`）

### 2.1 時間維度：多尺度時間窗

論文的時間窗長度：**0.2, 0.4, 0.7, 1.0, 1.25, 1.5 秒**，且 **50% overlap**。

實作方式：

* 先決定「單一 trial 可用長度」，例如 1.5 秒 → 對應樣本數 `L = 1.5 * fs = 375`
* 針對每個尺度 `w ∈ {0.2, 0.4, 0.7, 1.0, 1.25, 1.5}`：

  * `win_len = int(w * fs)`
  * `step = win_len // 2`
  * 建立所有起點 index（確保最後一個窗剛好 ending at L，論文也有提）。

你可以用一個結構 `time_windows` 存這些資訊：

```python
time_windows = [
    {"scale": 0.2, "start": start_idx, "stop": stop_idx, "window_id": ...},
    ...
]
```

### 2.2 頻率維度：多尺度頻帶

論文的頻率 segmentation：頻寬 **4, 8, 16, 32 Hz**，每個頻帶間 **50% overlap**，最後 4 Hz 頻帶到 30–34 Hz。

實作方式：

* 先定義你要覆蓋的大致頻帶（例如 4–40 Hz），再依照不同頻寬 + 50% overlap 去掃。
* 每個頻帶 ⟶ 一個 Butterworth bandpass filter（5 階），採用 **zero-phase filtering** (`filtfilt`) 以避免相位失真。

你可以建：

```python
freq_bands = [
    {"f_low": 4, "f_high": 8},
    {"f_low": 6, "f_high": 10},
    ...
]
```

### 2.3 空間維度：電極群組（channel groups）

論文做法：

* 對 BCI IV 2a（22 通道）定義 **10 組**電極群，前 8 組是手動依 ERD/ERS 對稱性設計（中央區、對側/同側等），後 2 組用 correlation-based channel selection 演算法自動選（8 & 12 channels）。
* SDU-MI（59 通道）則有 **20 組**，許多是對稱涵蓋 frontal / central / occipital 等區。

如果你先針對 BCI IV 2a：

* 在 `config.py` 手動定義每一組 channel 名稱（或 index）：

  ```python
  channel_groups = [
      {"id": 0, "name": "C3-Cz-C4", "ch_idx": [idx_C3, idx_Cz, idx_C4]},
      {"id": 1, "name": "central left/right", "ch_idx": [...]},
      ...
  ]
  ```
* 自動選擇的 group 可以晚點加；先把手動 group 做出來就可以跑完整 pipeline。

### 2.4 SFTS 定義

依論文 Algorithm 2：**總 SFTS 數量 N = Nc × Nf × Nt**。

你可以定義一個 `SFTSSpec` class 或 dict：

```python
class SFTSSpec:
    def __init__(self, ch_group_id, freq_band_id, time_window_id):
        self.ch_group_id = ch_group_id
        self.freq_band_id = freq_band_id
        self.time_window_id = time_window_id
```

`segmentation.generate_sfts_specs(channel_groups, freq_bands, time_windows)`
→ 返回 `sfts_specs: List[SFTSSpec]`，長度 N。

---

## 3. divCSP 特徵抽取模組（`divcsp.py` & `features.py`）

### 3.1 divCSP 核心（Algorithm 1）

論文中的目標：找一個正交矩陣 `R`，最大化 **類間 KL divergence**，同時最小化 **類內 KL divergence**，再由此得到空間濾波器。

* 先算兩類平均 covariance `Σ1, Σ2`
* `P = (Σ1 + Σ2)^(-1/2)` 作 whitening
* 在 whitened 空間上，用 gradient descent 在正交流形上更新 `R`（迭代）
* 最後得到 `W_sf`（空間濾波矩陣）

程式上可以分成兩個層次：

1. **先做一個簡化版：RCSP / 傳統 CSP**

   * 先用一般 CSP（或加上 L2 regularization）跑完 pipeline，確認 segmentation & wrapper selection 都工作正常。
2. **再替換成 divCSP：**

   * 將 J(R) 與梯度實作進 `pymanopt` 之類的 manifold optimizer
   * 或參考 Samek 的 divergence-based CSP（原文也就是引用那一套）。

**接口設計建議：**

```python
class DivCSP:
    def __init__(self, n_components=4, lambda_reg=0.1, max_iter=100):
        ...

    def fit(self, X, y):
        """
        X: (n_trials, n_channels, n_samples)
        y: (n_trials,), 0/1
        回傳: self.filters_  # shape (n_components, n_channels)
        """
        ...

    def transform(self, X):
        """
        回傳: features, shape (n_trials, n_components)
        features[i, k] = log-variance normalized of component k
        對應公式 (2)。:contentReference[oaicite:14]{index=14}
        """
        ...
```

注意：論文中 `d = 4`, `λ = 0.1`。

### 3.2 從單一 SFTS 生出特徵（`features.py`）

給定一個 `SFTSSpec` 和資料 `X`：

1. 根據 `spec.ch_group_id` 選取對應 channels
2. **頻率帶通**：

   * 真的要有效率：**先在整個 trial 維度對某個頻帶做濾波，再在時間窗裡切片**

     * 所以建議在外層先預先產一個 `X_fband[freq_band_id]`，每個是 `(n_trials, n_channels, n_samples)`，然後 SFTS 只作 index slice。
3. **時間窗切片**：

   * 取 `[start:stop]` 的樣本
4. 組合後得到 `X_sfts`: `(n_trials, n_sel_channels, n_samples_window)`
5. 呼叫 `DivCSP.fit`（在 training fold 上）→ `transform` 得到 features

   * 每個 trial 得到 `d=4` 維 feature

可以寫個 helper：

```python
def extract_sfts_features(X_fband, sfts_spec, divcsp_model, trial_idx):
    """
    X_fband: dict[freq_band_id] -> (n_trials, n_channels, n_samples)
    sfts_spec: SFTSSpec
    trial_idx: 本 fold 的 training or validation trial index
    """
    ...
    return features  # (n_trials_fold, d)
```

---

## 4. Wrapper-based SFTS 特徵選擇（`feature_selection.py`）

這一段就是論文 Algorithm 2。

### 4.1 單一 SFTS 的 LOBO 評估

對於每個 SFTS（總共有 N 個）：

1. 針對「該 SFTS」與所有 trial，先透過 divCSP 萃取 features（**但是 divCSP 要在每個 LOBO fold 的訓練集上重新 fit**，最嚴謹）。
2. 在每個 fold：

   * 取訓練 blocks → `X_train`, `y_train`
   * 取驗證 block → `X_val`, `y_val`
   * `divcsp.fit(X_train, y_train)`
   * `f_train = divcsp.transform(X_train)`
   * `f_val = divcsp.transform(X_val)`
   * 用 **Linear SVM**（scikit-learn `LinearSVC` 或 `SGDClassifier`）訓練在 `f_train`，在 `f_val` 評估 accuracy。
3. 所有 fold 的 accuracy 平均 → 就是這個 SFTS 的 `Acc[i]`。

**效率重點：**

* N 可能很大（幾千～上萬），每個 SFTS 上再做多折 CV 是很重的。可以：

  * 改成：先對這個 SFTS 在全部 training trial 上 fit 一次 divCSP，再配合 block-wise CV，只針對 SVM 重訓。這會有一點跟原文不同，但省非常多時間。
  * 或者用 **平行運算**：每個 SFTS 一個 worker（或每組 SFTS 批次），用 `joblib.Parallel`。

結束後你會有：

* `Acc`：shape `(N,)`，單一 SFTS 平均準確率
* `Seg`：長度 N 的 SFTSSpec list

### 4.2 排序與合併（D 與 K）

論文設 `D = 5`, `K = 3`：

1. 先依 `Acc` 由大到小排序 → 得到 `Acc_sorted`, `Seg_sorted`
2. 初始化 `AccMerged = []`
3. 迴圈 `j = D, 2D, 3D, ..., N`：

   * 取 top `j` 個 SFTS：`Seg_sorted[:j]`
   * 對這 `j` 個 SFTS，對每個 trial：

     * 用對應 SFTS 的 divCSP 特徵，**把所有 features 串接在一起**
       → 最後 feature 維度是 `j * d`
   * 用 LOBO 重做 Linear SVM cross-validation，算出平均 accuracy
   * 把結果存到 `AccMerged[count]`，同時記錄這個模型用到的 SFTS 集合與訓練好的 SVM。
4. 把 `AccMerged` 排序，取 **top-K**（K=3）個 ensemble 成員，存起來（每一個都包括：它自己的 SFTS 集合 + 對應的 CSP / SVM 參數）。

   * 這些就是「最終的 K 個子分類器」。

實作上可以定義：

```python
class EnsembleConfig:
    def __init__(self, sfts_indices, svm_model, csp_params_per_sfts):
        ...
```

並在 `feature_selection.py` 回傳：

```python
best_ensembles: List[EnsembleConfig]  # 長度 K
```

---

## 5. 線性 SVM Ensemble 與推論流程（`model.py`）

推論階段對應論文 Algorithm 3。

### 5.1 訓練接口

設計一個總模型：

```python
class FGSFTMIModel:
    def __init__(self, config):
        self.config = config

    def fit(self, X, y, blocks):
        """
        1. 產生 channel_groups, freq_bands, time_windows, sfts_specs
        2. 預先對每個 freq_band 做 bandpass 濾波 (cache)
        3. 跑單一 SFTS 評估 + merged SFTS 選擇 (Algorithm 2)
        4. 產生 best_ensembles (K 個)
        """
        ...

    def predict_proba(self, X):
        """
        對一批新的 trials，輸出兩類機率 (N_trials, 2)
        """
        ...

    def predict(self, X):
        return np.argmax(self.predict_proba(X), axis=1)
```

### 5.2 推論步驟

對於每筆 test trial：

1. 對每個頻帶做 bandpass（或直接用你訓練時 cache 的 filter 參數）
2. 對每一個 ensemble 成員 `Ci`：

   * 取 `Ci` 使用的那幾個 SFTS：

     * 根據 SFTSSpec：選 channel group → 頻帶資料 → 時間窗 slice
     * 用訓練好的 `DivCSP` 濾波器轉成 feature
   * 把所有 SFTS 的 feature 串接成一個 feature vector
   * 丟進 `Ci` 的 Linear SVM，拿到 class probability（`predict_proba` 或用 decision function + softmax）
3. 把 K 個 ensemble 成員的機率平均 → `ProbAvg`
4. 取 argmax → 預測 label

---

## 6. 計算效率優化建議

這篇方法本來就很重，想跑得動、跑得快，需要在程式層面好好設計：

### 6.1 頻帶濾波與時間窗重用

* **不要**對每個 SFTS 各自濾波一次，頻帶本來就可以共用：

  * 對每個 `(freq_band)` 在整個 `(n_trials, n_channels, n_samples)` 上做一次 `filtfilt`，
  * 把結果存成 `X_fband[freq_band_id]`，之後所有 SFTS 只做 channel & time slice。
* 時間窗也是靠 index 切，不要額外 copy，盡量用 view（例如 NumPy slicing）。

### 6.2 特徵計算與儲存

* 對「單一 SFTS + 所有 trial」，可以先在「全部 trial」上 fit 一次 CSP，再對 trial 計算 feature，然後在 LOBO 中只在 feature 空間上切訓練/驗證，避免每個 fold 重算 CSP（這是對原文做一點近似換取速度）。
* feature 存 `float32` 就夠用。
* 若記憶體吃緊，可以：

  * 先只算前 M 個 SFTS（例如根據某些 heuristic 選 e.g. 5k 個），
  * 或把 feature 分批存 HDF5，讀取時再載入。

### 6.3 平行化

* 單一 subject 的各個 SFTS 彼此獨立，非常適合 **平行處理**：

  * Python：用 `joblib.Parallel` 或 `multiprocessing.Pool`
  * 每個 worker 負責一批 SFTS index，計算 `Acc[i]`
* 注意隨機數種子（CSP 初始化、SVM）要固定，以方便重現。

### 6.4 實作順序建議（避免一次開太大）

1. 只做：

   * 時間 segmentation（T），不做頻率/空間 segmentation
   * 使用普通 CSP + 線性 SVM + LOBO
     → 先跑通 pipeline。
2. 加上頻率 segmentation（FT），但只用一小部分的頻帶。
3. 加上空間 segmentation（SFT），但 channel group 先定義幾組核心 sensorimotor group。
4. 最後再把 SFTS 數量、頻帶數量、channel group 數量逐步加上去，外加真正的 divCSP。

---

## 7. 額外實現：視覺化（MI-TFRM & topo map，`viz.py`，可選）

做論文裡的 **fine-grained MI-TFRM** 和 **time-frequency topographical map**：

* MI-TFRM：

  * 每一個 time × frequency 小格子對應到多個 SFTS（不同 channel group），
  * 用這些 SFTS 的 LOBO accuracy 加權平均當成「反應強度」，畫成 time-frequency heatmap。
* topo map：

  * 對某個 time × frequency 區段，統計使用到的 channel groups 的權重，投影到頭皮圖上（使用 MNE 的 `plot_topomap`）。

---
