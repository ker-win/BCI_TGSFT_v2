好，我直接幫你把「怎麼改到完全對齊 2–5 秒 MI 段」一步一步講清楚，照著改就可以了。

我們要達成的是：

> **每個 trial 的資料 = 從 cue 後 2 秒開始、長度 3 秒（2–5 秒 MI 段）**
> 然後 FGSFT 的時間窗全部都在這 3 秒裡面滑動。

---

## 步驟 0：決定 trial 長度（建議改成 3 秒）

論文說的是 **2–5 秒 MI 段（3 秒長度）**，所以建議把 `trial_len_sec` 改成 3.0：

```python
# config.py

@dataclass
class GlobalConfig:
    fs: float = 250.0
    trial_len_sec: float = 3.0  # ← 原本是 1.5，改成 3.0 對齊 2–5 秒整段
    ...
```
---

## 步驟 1：改 `load_subject_data` 的切段方式

目前 `data_loader.py` 是這樣取 trial：

```python
fs = 250.0

for i, etype in enumerate(events_type):
    if etype in mi_types:
        label = mi_types[etype]
        pos = events_position[i]
        ...
        t_start_sample = int(pos)
        t_end_sample   = int(pos + 4.0 * fs)
        ...
```

這代表每個 trial 是 **cue 後 0–4 秒**。
我們要改成：

> cue 的 sample index = `pos`
> trial = `pos + 2 秒` 開始，長度 = `cfg.trial_len_sec`（3 秒）
> → 時間範圍 = cue 後 2–5 秒

把這一段整個替換成下面版本：

```python
    fs = 250.0
    trial_len_sec = cfg.trial_len_sec     # 例如 3.0 秒
    mi_offset_sec = 2.0                   # 從 cue 後 2 秒開始（2–5 秒 MI 段）

    for i, etype in enumerate(events_type):
        if etype in mi_types:
            label = mi_types[etype]
            pos = events_position[i]      # 這個 etype (769/770/771/772) 的 sample index，視為 cue 時刻

            # 決定 block/run id（保持你原本的寫法）
            current_block = 0
            for b_idx, run_pos in enumerate(run_start_positions):
                if pos >= run_pos:
                    current_block = b_idx

            # ★ 這裡改成從 cue+2 秒開始，長度 = trial_len_sec
            t_start_sample = int(pos + mi_offset_sec * fs)
            t_end_sample   = int(pos + (mi_offset_sec + trial_len_sec) * fs)

            if t_end_sample <= raw_eeg.shape[1]:
                trial_data = raw_eeg[:, t_start_sample:t_end_sample]
                trials.append(trial_data)
                labels.append(label)
                block_ids.append(current_block)
```

這樣：

* `dataset.X` 裡每個 trial 的 shape 會變成：
  `n_samples = trial_len_sec * fs = 3.0 * 250 = 750`
* 每個 trial 的時間軸，就是「**MI 段 2–5 秒**」相對於 cue。

T 檔（訓練）和 E 檔（測試）都共用 `load_subject_data`，所以這個修改一做，**整個系統的 trial 定義就統一到 2–5 秒了**。

---

## 步驟 2：確保預處理不再亂改時間窗

現在 `FGSFTMIModel` 用的是你自己寫的 `Preprocessor`，而不是 `preprocess_pipeline`：

```python
# model.py
self.preprocessor = Preprocessor(fs_target=cfg.fs)
self.preprocessor.fit(dataset.X, dataset.fs)
ds_proc = self.preprocessor.transform(dataset)
```

`Preprocessor.transform()` 做的是：

1. `resample_to_fs`（重採樣）
2. 標準化（scaler）

**沒有再裁時間段**，所以 trial 仍然是 3 秒長度，不會被截短。
這樣和剛剛在 loader 裡切的 2–5 秒是完全相容的，不用再動。

唯一要注意的是：`preprocess_pipeline()` 這個舊函式裡還有「0.5 秒開始」那段註解，如果你未來不會再用它，可以：

* 直接無視（因為現在沒被 `model` 用到）；
* 或為避免混淆，清掉裡面那段 0.5s 的註解，把它標註成「deprecated / 不再使用」。

---

## 步驟 3：確認 Segmentation 是以 3 秒為總長度

`segmentation.generate_time_windows()` 裡：

```python
def generate_time_windows() -> List[TimeWindow]:
    fs = cfg.fs
    total_len_samples = int(cfg.trial_len_sec * fs)
    ...
```

你把 `trial_len_sec` 改成 3.0 之後：

* `total_len_samples = 3.0 * 250 = 750`
* 所有 time window 都會在 `[0, 750)` 這段範圍裡滑動
  → 等於在「2–5 秒 MI 段」的整個 3 秒上做 segmentation。

也就是說，**時間窗的基準點就是「MI 段起點 = cue+2 秒」**，這跟我們在 loader 裡的切法完美對齊。

---

## 步驟 4：簡單做幾個 sanity check（建議印一下）

改完後，你可以寫一個小檢查程式（例如加在某個 debug script 裡）：

```python
from data_loader import load_subject_data
from config import cfg

cfg.trial_len_sec = 3.0  # 確保現在是 3 秒設定

ds, meta = load_subject_data(1, file_suffix='T')
print("X shape:", ds.X.shape)        # (n_trials, 22, 750) 應該要是這樣
print("fs:", ds.fs)                  # 250.0
print("first block:", ds.blocks[0])

# 檢查 segmentation 總長度是否一致
from segmentation import generate_time_windows
tws = generate_time_windows()
print("max end_idx:", max(t.end_idx for t in tws))  # 應該 <= 750
```

如果你看到：

* `X.shape[-1] == 750`
* time windows 的 `end_idx` 都不會超過 750

就代表 **trial 的實際資料長度和 segmentation 的假設完全一致**。

---

## 小總結

要「完全對齊論文的 2–5 秒 MI 段」，你只要做三件事：

1. **把 trial 長度改成 3 秒：**

   ```python
   cfg.trial_len_sec = 3.0
   ```

2. **在 `load_subject_data` 裡，改成從 cue+2 秒開始切 trial，長度 = 3 秒：**

   ```python
   mi_offset_sec = 2.0
   t_start_sample = int(pos + mi_offset_sec * fs)
   t_end_sample   = int(pos + (mi_offset_sec + trial_len_sec) * fs)
   ```

3. **保持 Preprocessor 只做 resample + scaling，不再另外裁時間窗**（現在已經是這樣）。

這樣整個 pipeline—from T/E 檔讀進來的 trial、到 segmentation 的 time windows—就都在「cue 後 2–5 秒」這個 MI 段上運作了。

